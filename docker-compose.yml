version: '3'

services:
  app:
    build: gitolite-cgit
    image: bacnh85/gitolite-cgit
    container_name: gitolite-cgit
    env_file: config.env
    ports:
      - 22:22
    tty: true
    network_mode: bridge